import Hero from '../features/home/Hero';

const HomePage = () => {
    return (
        <div>
            <Hero />
            {/* Add more home sections here */}
        </div>
    );
};

export default HomePage;
